name: GitHub Trending 每日解读

on:
  schedule:
    # cron 用的是 UTC 时间。19:30 UTC = 墨尔本 AEST(UTC+10) 次日 05:30
    # 注意：墨尔本夏令时(10月初~4月初, AEDT=UTC+11)期间，这个时间会变成当地 06:30
    # 夏令时期间想保持 05:30，需改成 '30 18 * * *'
    - cron: '30 19 * * *'
    # 下面三次是补跑，专治订阅额度的 5 小时窗口被占满导致的 429。
    # 四次尝试横跨 6 小时，必定跨过一次窗口重置。
    # 日期标签用的是墨尔本时区（见下面的 date 步骤），所以这四次映射到同一个「墨尔本日」，
    # 已经发过 issue 的话后面几次会整个跳过，不会重复发。
    - cron: '30 21 * * *'   # 墨尔本 07:30
    - cron: '30 23 * * *'   # 墨尔本 09:30
    - cron: '30 1 * * *'    # 墨尔本 11:30（UTC 已翻页，但墨尔本仍是同一天）
  workflow_dispatch:   # 允许在网页上手动点一下就跑，方便测试
                       # ⚠️ 每点一次都实打实消耗订阅额度，别拿它当调试手段

permissions:
  contents: read
  issues: write

# 防止「定时触发」和「手动触发」撞在一起，重复发 issue 和邮件
concurrency:
  group: trending-digest
  cancel-in-progress: false

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 生成日期标签（issue 标题和邮件标题共用）
        id: date
        # 用墨尔本时区而不是 UTC：
        # 1) 让 19:30~01:30 UTC 这四次补跑落在同一个日期标签上，去重才成立
        # 2) 标题上的日期跟你早上读到它的当地日期一致
        run: echo "value=$(TZ=Australia/Melbourne date +%F)" >> "$GITHUB_OUTPUT"

      - name: 检查今天是否已发过
        id: check
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          DATE: ${{ steps.date.outputs.value }}
        run: |
          # 故意不用 `gh issue list --search`：GitHub 的搜索索引有延迟，
          # 补跑时刚发的 issue 可能还搜不到，会导致重复发。
          # 直接列最近 30 条走 REST API，本地用 jq 过滤，实时且可靠。
          count=$(gh issue list --state all --limit 30 --json title \
            --jq "[.[] | select(.title | contains(\"$DATE\"))] | length")
          if [ "$count" -gt 0 ]; then
            echo "::notice::$DATE 的解读已经发过了，跳过本次运行"
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: 生成解读并发布 issue
        if: steps.check.outputs.skip == 'false'
        uses: anthropics/claude-code-action@v1
        env:
          GH_TOKEN: ${{ github.token }}
        with:
          github_token: ${{ github.token }}

          # 用订阅额度。注意它和你本地跑 Claude Code 共享同一个 5 小时滚动窗口，
          # 所以务必去 Claude 设置里把 extra usage 打开——否则额度一满就是硬 429，
          # 日志里会看到 overageDisabledReason: "org_level_disabled"。
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # 哪天嫌撞车太频繁，注释掉上面一行、换成下面这行即可（二选一，不要同时设）：
          # anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}

          show_full_output: true
          claude_args: |
            --max-turns 20
            --allowedTools "WebFetch,Read,Write,Bash(gh issue create:*),Bash(cat:*)"
          prompt: |
            你要生成一份 GitHub Trending 的每日中文解读，读者是一名 **AI Agent 方向的入门学习者, 你不能刻意炫自己的理论水平有多高，而是较为详细地带着读者理解项目，提高技术水平，同时注意排版字体，不要让安卓手机端显示不全或者不美观**。
            ## 步骤
            1. 用 WebFetch 抓取 https://github.com/trending （Today、全部语言），
               提取**前三名**仓库的：全名（owner/repo）、主语言、总 star 数、今日新增 star 数、官方一句话描述。
            2. 对这三个仓库，分别抓取它们的 README 来真正理解项目内容：
               先试 https://raw.githubusercontent.com/<owner>/<repo>/main/README.md ，
               404 就再试 master 分支。抓不到就退回用仓库主页。
            3. 写一份 Markdown 解读，结构如下：
               - **前三名总览**：一个表格（排名 / 项目链接 / 语言 / stars / 今日新增 / 一句话），
                 表格后面用两三句话点出今天这三个项目**共同反映出什么趋势**。
               - **第 2 名、第 3 名**：各一小节，说清楚它解决什么问题、怎么解决的，
                 以及**对 Agent 入门者的启发**（列 2-4 条）。
               - **第 1 名详解**（这是重点，占全文一半以上篇幅）：
                 * 它到底是什么、怎么用（给出真实命令）
                 * 为什么今天能冲到第一（它相比已有方案好在哪）
                 * **架构拆解**：把它的实现拆成几个模块讲
                 * 每讲到一个 AI Agent 的通用知识点，就用引用块单独标注出来解释，例如：
                   > 📌 **知识点：Tool Use / Function Calling**
                   > （解释这个概念是什么、为什么重要、入门者该怎么理解）
                   常见可展开的点：工具调用、沙箱隔离、ReAct 循环、多 Agent 编排、
                   上下文工程、MCP、验证闭环、token 成本控制等——但只写这个项目**真正涉及**的，不要硬凑。
                 * 最后给一段「如果我要照着它学，该按什么顺序学」的建议
               - **结尾**：一个三列对比表，把三个项目放在一起看。
            4. 把这份 Markdown 用 gh CLI 发布成本仓库的一个 issue。
               先把正文用 Write 工具写到 /tmp/digest.md，再用 --body-file 引用，避免转义问题。
               标题里的日期**必须**用下面这个现成的值，不要自己调 date 命令算：
               gh issue create --title "GitHub Trending 解读 · ${{ steps.date.outputs.value }}" --body-file /tmp/digest.md
            ## 要求
            - 全文中文。
            - 内容必须具体：给真实命令、真实数字、真实结论。禁止写「这体现了 AI 的强大」这类空话。
            - 如果某个项目和 AI Agent 完全无关（比如是个字体库），照样如实介绍，
              但把「对 Agent 入门者的启发」换成一句诚实的说明，不要强行关联。
            - **/tmp/digest.md 是后续发邮件步骤的输入**，发完 issue 后不要删除或清空它。
            - 只做上面这些事。不要修改仓库里的任何文件，不要提交代码，不要开 PR。

      - name: 生成邮件正文（Markdown → HTML）
        if: steps.check.outputs.skip == 'false'
        run: |
          # 显式检查，避免 Claude 因额度/超时中断时，这里抛出一句莫名其妙的
          # "cp: cannot stat '/tmp/digest.md': No such file or directory"
          if [ ! -s /tmp/digest.md ]; then
            echo "::error::Claude 未生成 /tmp/digest.md（文件不存在或为空），跳过发信"
            exit 1
          fi
          sudo apt-get update -qq && sudo apt-get install -y -qq pandoc
          cp /tmp/digest.md "$GITHUB_WORKSPACE/digest.md"
          pandoc -f gfm -t html "$GITHUB_WORKSPACE/digest.md" -o "$GITHUB_WORKSPACE/digest.html"
          if [ ! -s "$GITHUB_WORKSPACE/digest.html" ]; then
            echo "::error::pandoc 产出的 HTML 为空"
            exit 1
          fi

      - name: 通过 Resend API 发送邮件
        if: steps.check.outputs.skip == 'false'
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
        run: |
          jq -n \
            --arg from "GitHub Trending Bot <onboarding@resend.dev>" \
            --arg to "brucelelele2000@outlook.com" \
            --arg subject "GitHub Trending 解读 · ${{ steps.date.outputs.value }}" \
            --rawfile html "$GITHUB_WORKSPACE/digest.html" \
            '{from:$from, to:[$to], subject:$subject, html:$html}' > /tmp/resend_payload.json

          http_status=$(curl -sS -o /tmp/resend_response.json -w '%{http_code}' \
            -X POST 'https://api.resend.com/emails' \
            -H "Authorization: Bearer $RESEND_API_KEY" \
            -H 'Content-Type: application/json' \
            -d @/tmp/resend_payload.json)

          cat /tmp/resend_response.json
          if [ "$http_status" -ge 300 ]; then
            echo "::error::Resend 返回了非成功状态码: $http_status"
            exit 1
          fi

      # 即使发信失败，也能从 Actions 页面把当天的 Markdown 下载下来，方便排查
      - name: 上传 digest 存档
        if: always() && steps.check.outputs.skip == 'false'
        uses: actions/upload-artifact@v4
        with:
          name: digest-${{ steps.date.outputs.value }}
          path: /tmp/digest.md
          if-no-files-found: ignore
          retention-days: 7
